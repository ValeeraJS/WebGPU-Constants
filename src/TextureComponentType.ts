enum TextureComponentType {
    FLOAT = "float",
    SINT = "sint",
    UINT = "uint",
    // Texture is used with comparison sampling only.
    DEPTH_COMPARISON = "depth-comparison",
}

export default TextureComponentType;
